name: 🌐 Deploy Frontend (HTML) - Somee.com

on:
  push:
    branches:
      - main
    # Chỉ chạy khi có thay đổi trong thư mục 'frontend/'
    paths:
      - 'frontend/**' 

jobs:
  deploy_frontend:
    runs-on: ubuntu-latest
    environment: production_frontend

    steps:
    - name: ⬇️ Checkout Mã Nguồn
      uses: actions/checkout@v4

    - name: 🚀 Triển khai qua FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.0 
      with:
        server: ${{ secrets.FE_HOST }}
        username: ${{ secrets.FE_USERNAME }}
        password: ${{ secrets.FE_PASSWORD }}
        
        # Đẩy thư mục cục bộ 'frontend'
        local-dir: './frontend/'
        
        # Đường dẫn đích trên host
        server-dir: ${{ secrets.FE_TARGET_DIR }}
        
        protocol: ftp 
        port: 21 
        
    - name: ✅ Triển khai Frontend hoàn tất
      run: echo "Frontend deployed to Somee.com successfully!"
